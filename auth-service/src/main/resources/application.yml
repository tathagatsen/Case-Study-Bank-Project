server:
  port: 9000
spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgreql://localhost:5432/authdb
    username: postgres
    password: postgresen
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:/db/migration 
